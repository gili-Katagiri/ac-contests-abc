#include<stdio.h>

#define rep(i,n) for(i=0;i<n;i++)

int dist[8][8];
int p[8];

int main(void){
    
    int n, k, i, j;

    scanf("%d %d". &n, &k);
    rep(i, n) rep(j,n) scanf("%d", &dist[i][j]);
    rep(i, n) p[i]=i;

    

    printf("%d\n", x);

    return 0;
}
